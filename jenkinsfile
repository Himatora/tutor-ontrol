pipeline {
    agent any

    environment {
        CMD = 'C:\\Windows\\System32\\cmd.exe'
        PM2_CMD = 'C:\\Users\\User\\node_modules\\.bin\\pm2.cmd'
        PYTHON_EXE = 'C:\\Users\\User\\AppData\\Local\\Programs\\Python\\Python313\\python.exe'
        SERVER_DIR = 'C:\\Users\\User\\Desktop\\jenkins\\tutor-ontrol_server'
        WORK_DIR = 'C:\\Users\\User\\Desktop\\jenkins\\tutor-ontrol'
    }

    triggers { 
        githubPush() 
    }

    stages {
        stage('Setup Git Safety') {
            steps {
                bat """
                    :: Добавляем безопасную директорию для Git
                    git config --global --add safe.directory "C:/Users/User/Desktop/jenkins/tutor-ontrol"
                    git config --global --add safe.directory "C:/Users/User/Desktop/jenkins/tutor-ontrol_server"
                """
            }
        }

        stage('Pull Latest Changes') {
            steps {
                withCredentials([
                    usernamePassword(credentialsId: 'github-creds', usernameVariable: 'GIT_USER', passwordVariable: 'GIT_TOKEN'),
                    string(credentialsId: 'github-email', variable: 'GIT_EMAIL')
                ]) {
                    bat """
                        cd "${WORK_DIR}"
                        
                        :: Настраиваем git для этой директории
                        git config user.name "%GIT_USER%"
                        git config user.email "%GIT_EMAIL%"
                        
                        :: Получаем последние изменения
                        git fetch origin
                        git checkout fix
                        git reset --hard origin/fix
                        
                        echo "Changes pulled successfully from fix branch"
                        
                        :: Копируем только нужные файлы, исключая node_modules и другие
                        robocopy "${WORK_DIR}" "${SERVER_DIR}" /E /XF package-lock.json /XD node_modules __pycache__ .git .vscode
                    """
                }
            }
        }

        stage('Install Dependencies') {
            steps {
                bat """
                    echo "Installing Python dependencies..."
                    cd "${SERVER_DIR}"
                    "${PYTHON_EXE}" -m pip install -r requirements.txt || echo "Python dependencies installed with warnings"

                    echo "Installing frontend dependencies..."
                    cd "${SERVER_DIR}\\client"
                    call npm install || echo "NPM dependencies installed with warnings"
                    
                    echo "Dependencies installation completed"
                """
            }
        }

        stage('Stop Existing Servers') {
            steps {
                bat """
                    echo "Stopping existing servers..."
                    call "${PM2_CMD}" delete django || echo "No Django process to stop"
                    call "${PM2_CMD}" delete vue || echo "No Vue process to stop"
                    timeout /t 5 /nobreak > nul
                """
            }
        }

        stage('Start Backend Server') {
            steps {
                bat """
                    echo "Starting backend server..."
                    cd "${SERVER_DIR}"

                    :: Запускаем бекенд
                    call "${PM2_CMD}" start "${PYTHON_EXE}" --name django -- manage.py runserver 127.0.0.1:8000
                    timeout /t 10 /nobreak > nul
                    
                    echo "Backend server started"
                    call "${PM2_CMD}" list
                """
            }
        }

        stage('Start Frontend Server') {
            steps {
                bat """
                    echo "Starting frontend server..."
                    cd "${SERVER_DIR}\\client"

                    :: Запускаем фронтенд
                    call "${PM2_CMD}" start "${CMD}" --name vue -- /c "cd ${SERVER_DIR}\\client && npm run dev"
                    timeout /t 10 /nobreak > nul

                    echo "Frontend server started"
                    call "${PM2_CMD}" list
                """
            }
        }

        stage('Run Tests') {
            steps {
                script {
                    try {
                        bat """
                            cd "${SERVER_DIR}"
                            echo "Running Django tests..."
                            "${PYTHON_EXE}" manage.py test --noinput
                        """
                        echo "✅ All Django tests passed!"
                    } catch (err) {
                        echo "❌ Tests failed! Stopping servers..."
                        bat """
                            call "${PM2_CMD}" delete django || echo "No Django process to delete"
                            call "${PM2_CMD}" delete vue || echo "No Vue process to delete"
                        """
                        error("Django tests failed. Servers stopped.")
                    }
                 }
            }
        }

        stage('Merge and Sync Branches') {
            when {
                expression { currentBuild.result == null || currentBuild.result == 'SUCCESS' }
            }
            steps {
                withCredentials([
                    usernamePassword(credentialsId: 'github-creds', usernameVariable: 'GIT_USER', passwordVariable: 'GIT_TOKEN'),
                    string(credentialsId: 'github-email', variable: 'GIT_EMAIL')
                ]) {
                    bat """
                        cd "${WORK_DIR}"
                        
                        :: Настраиваем git
                        git config user.name "%GIT_USER%"
                        git config user.email "%GIT_EMAIL%"

                        :: Переключаемся на main и обновляем
                        git checkout main
                        git pull origin main

                        :: Мержим fix в main
                        git merge fix --no-ff -m "Merge fix branch [Jenkins Build: ${BUILD_NUMBER}]"

                        :: Пушим изменения
                        git push origin main

                        :: Синхронизируем fix с main
                        git checkout fix
                        git merge main --no-edit
                        git push origin fix

                        echo "✅ Branches merged and synced successfully"
                    """
                }
            }
        }

        stage('Verify Servers') {
            steps {
                bat """
                    echo "Verifying servers are running..."
                    call "${PM2_CMD}" list
                    
                    echo "✅ Pipeline completed successfully!"
                    echo "🌐 Backend should be at: http://127.0.0.1:8000/"
                    echo "🖥️ Frontend should be at: http://localhost:5173/"
                """
            }
        }
    }

    post {
        always {
            echo "Pipeline execution completed with status: ${currentBuild.result}"
            bat """
                call "${PM2_CMD}" list
            """
        }
        success {
            echo "🎉 Pipeline completed successfully!"
            echo "📝 Changes merged to main and fix branches"
        }
        failure {
            echo "❌ Pipeline failed! Check the logs above."
        }
    }
}